name: Java to GCP Workflow

on:
  push:

jobs:
  Java-Workflow:
    uses: DevOpsORT-proyecto/obligatorioDevOps-infra/.github/workflows/Java-Workflow.yml@main
    with:
      service-name: Shipping
      sonar-project-key: DevOpsORT-proyecto_orders-service
    secrets: inherit